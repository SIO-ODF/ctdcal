Summary:
Package to process CTD data from raw data to data product to be submitted to a data repository, ex. CCHDO. run.py will eventually be a superscript allowing for an analyst to push one button to process data. The other scripts in this directory are the subscripts called from run.py

run.py
Currently not working, to be fixed in future

---

converter.py

Convert SBE CTD data into engineering units

usage: converter.py [-h] [-o output file] [-d] [-w [dest dir]] hex File XMLCON File

positional arguments:
  hex File        the .hex data file to process
  XMLCON File     the .XMLCON data file to process

optional arguments:
  -h, --help      show this help message and exit
  -o output file  name and location of output file
  -d, --debug     display debug messages
  -w [dest dir]   build web viewer, optionally specify the destination
                  directory for the webviewer files

---

bottle.py

Create a bottle file for fitting

usage: bottle.py [-h] [-o output file] [-d] converted_file

positional arguments:
  converted_file  the .converted file to process

optional arguments:
  -h, --help      show this help message and exit
  -o output_file  name and location of output file
  -d, --debug     display debug messages

---

Dependency requirements:
python3
numpy
scipy
gsw

08 Nov 2016
